apiVersion: v1
kind: Service
metadata:
  name: messages
  labels:
    app: messages
spec:
  selector:
    app: messages
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: {{ .Values.containerPort }}
  type: ClusterIP
