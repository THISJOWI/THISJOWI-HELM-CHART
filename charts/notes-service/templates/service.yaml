apiVersion: v1
kind: Service
metadata:
  name: notes
  labels:
    app: notes
spec:
  selector:
    app: notes
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: {{ .Values.containerPort }}
  type: ClusterIP
