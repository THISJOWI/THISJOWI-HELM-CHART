apiVersion: v1
kind: Service
metadata:
  name: password
  labels:
    app: password
spec:
  selector:
    app: password
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: {{ .Values.containerPort }}
  type: ClusterIP
